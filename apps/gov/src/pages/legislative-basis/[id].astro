---
import PublicationLoader from '@/components/PublicationLoader.astro'

export const prerender = false

const id = Astro.params.id as string
---

<PublicationLoader {id} kind="legal_document" locale="uk" />
