---
import type { Metadata, ContentSchema } from '@babyn-yar/schema'
import { ContentAPI } from '@babyn-yar/api-utils'
import CMSList from '@/components/CMSList.astro'

export const prerender = false

const page = Number(Astro.url.searchParams.get('page') || '1')
const response = await ContentAPI.list<{
  articles: ContentSchema.Content[]
  metadata: Metadata
}>({ slug: 'media-articles', filters: { page, lang: 'ua' } })
---

<CMSList
  title="ЗМІ про заповідник"
  description="ЗМІ про заповідник"
  slug="media-articles"
  list={response.articles}
  metadata={response.metadata}
/>
