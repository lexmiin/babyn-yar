---
import AudioGuide from '@/components/AudioGuide.astro'
import { getAudioGuide } from '@/components/audio-guide'
export function getStaticPaths() {
  return getAudioGuide('en').stops.map(({ id }) => ({ params: { id } }))
}
---

<AudioGuide lang="en" id={Astro.params.id} />
