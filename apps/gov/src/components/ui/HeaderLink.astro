---
interface Props {
  href?: string
  target?: string
  rel?: string
}

const { href, target, rel } = Astro.props
const Element = href ? 'a' : 'button'
---

<Element
  href={href}
  {target}
  {rel}
  class="block w-full hover:bg-black hover:text-white"
>
  <slot />
</Element>
