---
import { ContactForm as ContactFormReact } from '@/components/ui'
import { getLangFromUrl } from '@babyn-yar/i18n'
import { EMAIL } from '@/constants'

const lang = getLangFromUrl(Astro.url)
---

<ContactFormReact client:load lang={lang} email={EMAIL} />
